{% if ctaLink %}
  <a class="button button--green min-w-[180px]" href="{{ ctaLink.url }}" {% if ctaLink.target %} target="{{ ctaLink.target }}" rel="noreferrer noopener"{% endif %}>
    {{ ctaLink.title|e }}
  </a>
{% endif %}
